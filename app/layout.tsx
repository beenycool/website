import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "./components/ThemeProvider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Dr Moustafa Saoud | Consultant Forensic Psychiatrist",
  description:
    "Supporting mental health journeys with professional, confidential, and compassionate care. Expert forensic psychiatry, medicolegal reports, and psychotherapy.",
  metadataBase: new URL("https://example.com"),
  openGraph: {
    title: "Dr Moustafa Saoud | Consultant Forensic Psychiatrist",
    description:
      "Supporting mental health journeys with professional, confidential, and compassionate care.",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning className="light">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <ThemeProvider>
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
